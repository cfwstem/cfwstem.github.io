 var LoadScript = function() {
           var options = {
                valueNames: [ 'Name', 'Department', 'Position' ]
                          };
    /* global List */
            var userList = new List('users', options);
 }